# DBProject2
This poject allows employees to interact with a parking system database using express. 

# Running the program
Install node and npm, run the command "npm install bcrypt express jsonwebtoken knex mysql" to install the necessary dependancies, then run the command "node index.js" to execute the program. The app will run on localhost:3000. 
